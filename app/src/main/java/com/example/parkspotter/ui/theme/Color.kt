package com.example.parkspotter.ui.theme

import androidx.compose.ui.graphics.Color

// ── Sistema de diseño ParkSpotter (Manual de Identidad Visual 2026) ──────────
// Azul marino = confianza, seguridad, tecnología
// Verde menta = disponibilidad, movilidad, crecimiento
val BluePrimary    = Color(0xFF0B2D5B)  // Azul marino primario (antes 0xFF1D4ED8)
val BlueLight      = Color(0xFF1E4A85)  // Azul hover / variante intermedia
val Blue50         = Color(0xFFE7EDF5)  // Azul muy claro
val SurfaceWhite   = Color(0xFFFFFFFF)  // Blanco puro
val BackgroundGray = Color(0xFFF4F7FA)  // Gris muy claro (igual al del manual de marca)
val TextDark       = Color(0xFF0F172A)  // Texto principal
val TextMedium     = Color(0xFF475569)  // Texto secundario
val TextLight      = Color(0xFF94A3B8)  // Texto terciario
val BorderGray     = Color(0xFFCBD5E1)  // Bordes
val GreenSuccess   = Color(0xFF059669)  // Estado "disponible"
val RedError       = Color(0xFFEF4444)  // Ocupado
val YellowWarning  = Color(0xFFF59E0B)  // Reservado

// Acento de marca (logo, botones primarios de acción, highlights)
val GreenAccent     = Color(0xFF00C896)  // Verde menta principal del logo
val GreenAccentDark = Color(0xFF00A67D)  // Texto/ícono verde sobre fondo claro